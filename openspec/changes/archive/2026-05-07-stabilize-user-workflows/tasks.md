## 1. 任务错误详情增强

- [x] 1.1 梳理任务失败写入路径，确认当前 error/message/details 字段来源。
- [x] 1.2 定义统一失败载荷：`summary` / `technical_detail` / `category` / `suggestions`。
- [x] 1.3 后端失败写库时保留真实异常文本并做敏感信息脱敏。
- [x] 1.4 前端任务中心错误弹窗展示真实摘要和技术细节。
- [x] 1.5 增加至少 1 个后端回归测试，覆盖真实错误不会被泛化覆盖。

## 2. 模拟交易导入模板/粘贴导入

- [x] 2.1 在导入弹窗增加粘贴区和示例模板。
- [x] 2.2 实现本地解析：支持 tab、逗号、连续空格分隔。
- [x] 2.3 支持有表头和无表头两种输入。
- [x] 2.4 解析后填充导入表格，用户确认后继续走 `/api/paper/import`。
- [x] 2.5 增加解析逻辑的单元测试或最小可验证测试。

## 3. 前端 type-check 清零

- [x] 3.1 修复 `frontend/src/views/Dashboard/index.vue` 现有未使用变量。
- [x] 3.2 运行 `npm run type-check -- --pretty false`，确认退出码为 0。

## 4. 收口验证

- [x] 4.1 后端相关测试通过。
- [x] 4.2 前端类型检查通过。
- [x] 4.3 服务健康检查通过。
- [x] 4.4 更新 changelog 或开发记录。
